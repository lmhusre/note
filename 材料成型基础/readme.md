# 材料成型基础笔记协作说明

这份笔记的主文件是 `材料成型基础.tex`，采用 LaTeX 维护。

## 当前状态

从现有正文结构看，这份笔记目前主要覆盖：

- 金属材料技术基础
- 金属材料的主要性能
- 铁碳合金的状态相图
- 钢的分类和编号
- 钢的热处理

整体上，这是一份仍在生长中的课程笔记，篇幅比其他项目短，后续很可能继续扩展章节。

## 目录结构

- `材料成型基础.tex`
  - 正文主文件。
- `auxil/`
  - 编译中间文件与日志目录。
- `out/`
  - 编译产物目录。

## 维护原则

### 1. 保持骨架清晰

- 当前篇幅不长，章节层级比较清楚，后续扩写时不要破坏现有 `\section` / `\subsection` / `\subsubsection` 结构。
- 如果新增章节，优先保证目录可读、层级统一。

### 2. 内容风格建议

- 这份笔记更适合写成“概念 + 对比 + 规律 + 典型表格”的形式。
- 如果引入更多表格，注意表头、单位和对齐方式统一。
- 如果补充图示或相图，建议优先选择清晰、简洁、可解释的版本，不要堆无关大图。

### 3. 生成目录不要手改

- `auxil/` 和 `out/` 是生成目录，不是正文目录。
- 真正需要维护的是 `材料成型基础.tex`。

## 编译方式

推荐在工作区根目录运行：

```bat
D:\latex0223\run_latex_watch.bat "D:\latex0223\材料成型基础\材料成型基础.tex" --once
```

结果：

- PDF 输出到 `材料成型基础/out/`
- 日志输出到 `材料成型基础/auxil/`

## 协作提醒

- 这份笔记目前更像一个已搭好版式的课程框架，后续扩写时请优先保证知识点组织清楚，而不是一开始就追求花哨排版。
- 如果未来把本课程补成完整讲义，建议同步在这里登记“已整理到哪一章”。

## 交接前检查

- 正文能否成功编译
- 表格和目录是否正常
- 是否误改了生成文件
- 是否为下一位协作者留下了明确的补充位置
